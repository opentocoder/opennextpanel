# 使用 Node.js 官方镜像
FROM node:20-slim

# 设置工作目录
WORKDIR /opt/opennextpanel

# 复制 package 文件
COPY package*.json ./

# 安装依赖
RUN npm install --legacy-peer-deps

# 复制源代码
COPY . .

# 构建
RUN npm run build

# 暴露端口
EXPOSE 3000

# 启动
CMD ["npm", "start"]
