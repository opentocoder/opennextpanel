# OpenNextPanel 环境变量配置示例
# 复制此文件为 .env 并修改配置

# ============================================================
# 安全配置（必须设置）
# ============================================================

# JWT 密钥 - 必须设置！用于签名认证令牌
# 生成方式: openssl rand -base64 32
JWT_SECRET=your-secret-key-here-change-it

# ============================================================
# 面板管理员配置
# ============================================================

# 管理员密码（可选）
# - 如果不设置，安装时会自动生成随机密码并写入此处
# - 也可以提前设置，安装时将使用此密码
# - 修改此值后需要运行 node scripts/init-db.js 更新密码
ADMIN_PASSWORD=

# ============================================================
# MySQL/MariaDB 数据库配置
# ============================================================

# MySQL 连接配置
MYSQL_HOST=localhost
MYSQL_PORT=3306

# MySQL root 用户配置
# - 安装 MariaDB/MySQL 时会自动生成 root 密码并写入此处
# - 也可以提前设置，安装时将使用此密码
MYSQL_ROOT_USER=root
MYSQL_ROOT_PASSWORD=

# ============================================================
# Docker 网络配置
# ============================================================

# OpenNextPanel 使用的 Docker 网络名称
# 所有面板管理的容器都会加入此网络
DOCKER_NETWORK=opennextpanel-network

# ============================================================
# 运行环境
# ============================================================

# 运行模式: development | production
NODE_ENV=production

# 面板端口（默认 8888）
PORT=8888
